import {SilentMessage} from "./SilentMessage";
import {BSTAlexa} from "bespoken-tools";

export class MessageHandler {

    public constructor() {}

    public handle(message: SilentMessage) {

    }
}